export async function processYoutubeUrl(url) {
  // Simulate processing YouTube URL
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve([
        Math.random(),
        Math.random(),
        Math.random(),
        Math.random()
      ]);
    }, 1000);
  });
}